const template = document.createElement("template")

template.innerHTML = `
<div id="staff-wrapper" class="wrapper top-as-padding bottom-as-padding">
    <div id="staff"></div>
    <app-paginator total-items="0"></app-paginator>
<div/>
`

export default template
